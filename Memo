might need an array for the angles history 



To avoid manual ROI selection, might be better to use special colors for the paper that normally doesn't appear on the snooker table, that way there is no need for a particular ROI and only birds eye view is necessary

On the paper device, try to make the paper fit tightly to the cue, also think of other materials that work better

final_score calculation in the function find_optimal_threshold need some testing

add in a baseline angle at the start of frame and adjust later angles to be relative to starting position
(this assumes initial position is good, but using other methods like CB-OB line might cause other inaccuracies)

change the buffer window to using actual time elapsed, now is using buffersize = 5 but fps will change its robustness. calculate buffer to span maybe 0.1 seconds for any fps smoothing to work.

Add an automatic outlier rejection: if previous angle output and current angle output differ by more than 5 degrees (up for more scrutiny) then it must be faulty, drop or guess the value in this case.

Move the code related to creating plots into cueAngleEngine since that is where the data is stored and reduces cluttering.